create database smartuniversity;
use smartuniversity;
create table usuario
(
id int NOT NULL PRIMARY KEY AUTO_INCREMENT,
nom VARCHAR(30),
app VARCHAR(20),
apm VARCHAR(20),
matricula int,
tipo int,
correo VARCHAR(50),
pass VARCHAR(100)
);

--0->admin, 1->estudiante, 3->adminLoboBici

INSERT INTO usuario (nom, app, apm, matricula, tipo, correo, pass) VALUES
('Abdiel', 'Pérez', 'Balcón', 201931429, 0, 'abdiel.perezb@alumno.buap.mx', '1234');